{
  "buildCommand": "npm run build",
  "installCommand": "npm install && npx prisma generate && npm run seed:deployment-seed",
  "env": {
    "NEXTAUTH_URL": "https://www.laurentgagne.com",
    "NODE_ENV": "production"
  }
}
