{
  "buildCommand": "npm run build && npx prisma generate && npm run seed:deployment-seed",
  "installCommand": "npm install",
  "env": {
    "NEXTAUTH_URL": "https://www.laurentgagne.com",
    "NODE_ENV": "production"
  }
}
